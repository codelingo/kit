tenets:
  - name: find-funcs
    flows:
      codelingo/review:
        comment: This is a function, but you probably already knew that.
    query: |
      import codelingo/ast/go

      @review comment
      go.func_decl(depth = 4)
